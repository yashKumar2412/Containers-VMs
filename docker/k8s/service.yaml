apiVersion: v1
kind: Service
metadata:
  name: contact-mgmt-service
  labels:
    app: contact-mgmt
spec:
  type: NodePort
  selector:
    app: contact-mgmt
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
    nodePort: 30000